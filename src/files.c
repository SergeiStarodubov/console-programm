#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <stdbool.h> 
#include <ctype.h>
#include <time.h>
#include <unistd.h>

int main() {
    int account;
    char name[30];
    double balance;

    FILE* ptr;

    if ((ptr = fopen("clients.dat", "w") == NULL)) puts("the file is not found");
    else {
        puts
    }
    return 0;
}